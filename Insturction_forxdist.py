"""
Для запуска парарельных тестов используется плагие pytest-xdist

Что бы запустить тесты нужно добавить флаг --numprocesses=тут указать количество воркеров либо коротко --n=

1 воркер занимает 1 ядро

можно указать --numprocesses=auto и тогда количество воркеров будет создано автоматиски равное количеству ядерз

python -m pytest -m "regression" --numprocesses=


 python -m pytest -m "regression" --numprocesses=2




---

## Почему “быстрые” тесты не дают профита от параллелизма

### 1) Время уходит на распределение тестов между воркерами

Главный процесс pytest должен:

* собрать список тестов,
* “нарезать” их воркерам,
* отправлять задания,
* получать результаты обратно.

Если каждый тест длится, условно, **20–50 мс**, то “админка” становится заметной долей времени.

---

### 2) Дорогой запуск воркеров (процессов)

Каждый воркер — это **отдельный процесс Python**, ему нужно:

* стартануть,
* импортировать проект/зависимости,
* поднять плагины,
* инициализировать окружение.

Для 5 воркеров это умножается на 5.
Если весь прогон и так короткий — “стартовые расходы” съедают выгоду.

---

### 3) Фикстуры/инициализация повторяются в каждом воркере

То, что в обычном прогоне делалось **1 раз**, в xdist может делаться **в каждом процессе**:

* создание клиентов
* чтение конфигов
* авторизация
* подготовка данных

Особенно “дорого”, если есть `session`-фикстуры: в xdist они **session внутри воркера**, а не “одна на весь прогон”.

---

### 4) Тестов мало → воркеры простаивают

Если тестов 19, а воркеров 5:

* часть воркеров быстро закончит и будет стоять
* балансировка не успевает “раскрыться”

Параллелизм выгоден, когда тестов **много** и есть что равномерно распределять.

---

### 5) Есть общий “узкий ресурс”

Даже при 10 воркерах ты можешь упереться в:

* один API сервер (он просто не отвечает быстрее)
* rate limit
* одну базу/кэш/очередь
* сеть/локальный комп

То есть “параллелим”, а быстрее не становится — потому что тормозит внешний ресурс.

---

### 6) Конфликты данных = ретраи/флаки → медленнее

Если тесты не изолированы (одни и те же юзеры/курсы/записи):

* появляются гонки
* больше падений/перезапусков
* больше времени на разбор

---

## Запоминалка (1 строка)

**Параллелизм даёт выигрыш, когда “работа тестов” заметно больше, чем “стоимость запуска и управления воркерами”.**

Если хочешь — скажи сколько у тебя примерно тестов и средняя длительность одного, я подскажу, с какого `-n` обычно начинать (2/4/auto) и что проверять, чтобы увидеть ускорение.

"""